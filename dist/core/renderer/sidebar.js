export function renderSidebar(items, currentSlug, locale = "en") {
    const currentHref = locale === "en" ? `/${currentSlug}` : `/${locale}/${currentSlug}`;
    return items
        .map((item) => {
        if (item.items && item.items.length > 0) {
            const hasActive = item.items.some((sub) => sub.href === currentHref || sub.href === `/${currentSlug}`);
            const categoryItems = item.items
                .map((sub) => {
                const isActive = sub.href === currentHref || sub.href === `/${currentSlug}`;
                const autoBadge = sub.autoGenerated ? ' <span class="auto-badge" title="Auto-generated">⚡</span>' : '';
                return `<a href="${sub.href}" class="sidebar-link preload ${isActive ? "active" : ""}">${sub.label}${autoBadge}</a>`;
            })
                .join("");
            const autoBadge = item.autoGenerated ? ' <span class="auto-badge" title="Auto-generated">⚡</span>' : '';
            return `
          <div class="sidebar-category ${hasActive ? "" : "collapsed"}">
            <div class="sidebar-category-header preload">
              <span>${item.label}${autoBadge}</span>
              <svg class="sidebar-category-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <polyline points="6 9 12 15 18 9"></polyline>
              </svg>
            </div>
            <div class="sidebar-category-items">
              ${categoryItems}
            </div>
          </div>
        `;
        }
        const isActive = item.href === currentHref || item.href === `/${currentSlug}`;
        const autoBadge = item.autoGenerated ? ' <span class="auto-badge" title="Auto-generated">⚡</span>' : '';
        return `<a href="${item.href}" class="sidebar-link preload ${isActive ? "active" : ""}">${item.label}${autoBadge}</a>`;
    })
        .join("");
}
//# sourceMappingURL=sidebar.js.map